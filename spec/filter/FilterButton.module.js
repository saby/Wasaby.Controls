define('js!SBIS3.SPEC.filter.FilterButton', [
], function() {

   /**
    * Кнопка фильтров.
    * @class SBIS3.SPEC.filter.FilterButton
    * @extends SBIS3.SPEC.Control
    * @control
    * @public
    */


   /**
    * @name SBIS3.SPEC.filter.FilterButton#filter
    * @cfg {Object} Текущее значение фильтра.
    */

   /**
    * @name SBIS3.SPEC.filter.FilterButton#template
    * @cfg {String} Имя компонента, в котором происходит настройка фильтра.
    */

   /**
    * @event SBIS3.SPEC.filter.FilterButton#onFilterChange Происходит при изменении фильтра.
    * @param {Object} filter Новое значение фильтра.
    */

});