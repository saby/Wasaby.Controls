define('js!SBIS3.SPEC.interface.ICheckable', [
], function() {

   /**
    * Интерфейс работы переключетеля.
    * @mixin SBIS3.SPEC.interface.ICheckable
    * @public
    */

   /**
    * @name SBIS3.SPEC.interface.ICheckable#checked
    * @cfg {Boolean} Устанавливает состояние переключателя.
    */

   /**
    * @event SBIS3.SPEC.interface.ICheckable#onChangeChecked Происходит при изменении состояния переключателя.
    * @param {Boolean} value Новое состояние.
    */

});