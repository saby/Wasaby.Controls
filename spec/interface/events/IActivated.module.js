define('js!SBIS3.SPEC.interface.IActivated', [
], function() {

   /**
    * Интерфейс события активации.
    * @mixin SBIS3.SPEC.interface.IActivated
    * @public
    */

   /**
    * @event SBIS3.SPEC.interface.ICheckable#onActivated Происходит при изменении состояния переключателя.
    */

});