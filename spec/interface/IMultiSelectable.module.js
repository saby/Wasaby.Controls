define('js!SBIS3.SPEC.interface.IMultiSelectable', [
], function() {

   /**
    * Интерфейс работы списков.
    * @mixin SBIS3.SPEC.interface.IMultiSelectable
    * @public
    */

   /**
    * @name SBIS3.SPEC.interface.IMultiSelectable#selectedKeys
    * @cfg {Array} Ключевое поле выбранного элемента коллекции.
    */

   /**
    * @event SBIS3.SPEC.interface.IInputText#onChangeSelectedKeys Происходит при изменении массива выделенных записей.
    * @param {Array} keys Текст в поле ввода.
    */

});