define('js!SBIS3.SPEC.interface.ITriCheckable', [
], function() {

   /**
    * Интерфейс работы трехпозиционного переключетеля.
    * @mixin SBIS3.SPEC.interface.ITriCheckable
    * @public
    */

   /**
    * @name SBIS3.SPEC.interface.ITriCheckable#checked
    * @cfg {Boolean|undefined} Устанавливает состояние переключателя.
    */

   /**
    * @event SBIS3.SPEC.interface.ITriCheckable#onChangeChecked Происходит при изменении состояния переключателя.
    * @param {Boolean|undefined} value Новое состояние.
    */

});