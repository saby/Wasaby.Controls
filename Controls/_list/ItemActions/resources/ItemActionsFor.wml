<ws:for data="action in showedActions">
    <ws:partial
            template="wml!Controls/_list/ItemActions/resources/ItemAction"
            itemData="{{itemData}}"
            size="{{action._isMenu ? 'm' : size}}"
            action="{{action}}"/>
</ws:for>
