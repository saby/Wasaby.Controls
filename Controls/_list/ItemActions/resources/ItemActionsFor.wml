<ws:for data="index, action in showedActions">
    <ws:partial
            template="wml!Controls/_list/ItemActions/resources/ItemAction"
            itemData="{{itemData}}"
            size="{{size}}"
            isFirst="{{index === 0}}"
            action="{{action}}"/>
</ws:for>
