<Controls.Container.Async templateName="Controls/toggle:CheckboxMarker">
    <ws:partial template="{{content.resolvedTemplate}}"
                attr:class="{{ (gridColumn || colData || item || itemData).getMultiSelectClasses ? (gridColumn || colData || item || itemData).getMultiSelectClasses(theme, backgroundColorStyle, cursor, highlightOnHover) : '' }}"
                triState="{{triState}}"
                readOnly="{{checkboxReadOnly || false}}"
                value="{{multiSelectStatus}}"
                on:click="_onCheckBoxClick(key, multiSelectStatus, checkboxReadOnly)"
    />
</Controls.Container.Async>
