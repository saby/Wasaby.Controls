<div attr:class="controls-Label">
   <div attr:class="controls-Label__caption_{{isEnabled ? 'enabled' : 'disabled'}}
                    {{_options.requered ? ' controls-Label__caption_required'}}
                    ws-flexbox ws-align-items-center">{{_options.caption}}</div>
</div>