import * as template from 'wml!Controls/_grid/layout/common/Control';
import {Control, TemplateFunction} from 'UI/Base';

export default class ForControl extends Control {
    protected _template: TemplateFunction = template;
}
