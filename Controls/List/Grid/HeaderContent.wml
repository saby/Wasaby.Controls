<div class="controls-Grid__header-cell__content controls-Grid__row-header__content_baseline">
    <ws:if data="{{colData.column.sortingProperty}}">
        <Controls.List.Grid.SortButton
                caption="{{colData.column.title}}"
                property="{{colData.column.sortingProperty}}"
                type="{{colData.column.sortingType}}"
                value="{{colData.sortingDirection}}"/>
    </ws:if>
    <ws:else>{{colData.column.title}}</ws:else>
</div>