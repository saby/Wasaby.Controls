<div class="ws-DigitButtons">
<ws:for data="digit in _digits">
   <ws:if data="{{digit == '...'}}">
      <span class="ws-Paging__btn ws-DigitButtons__ellipsis">{{digit}}</span>
   </ws:if>
   <ws:else>
      <span on:click="__digitClick(digit)" class="ws-Paging__btn ws-DigitButtons__digit{{digit == _options.selectedKey ? ' ws-DigitButtons__digit__selected' : ' ws-DigitButtons__digit__unselected'}}">{{digit}}</span>
   </ws:else>
</ws:for>
</div>