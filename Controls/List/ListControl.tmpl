<div class="ws-ListControl">
   <div class="ws-ListControl__topListTrigger"></div>
   <div class="ws-ListControl__topPlaceholder">
      <div class="ws-ListControl__topLoadTrigger"></div>
   </div>

   <ws:Controls.List.ListControl.ListView
         items="{{_items}}"
         itemTemplate="{{_options.itemTemplate}}"
         idProperty="{{_options.idProperty}}"
         displayProperty="{{_options.displayProperty}}"
         selectedKey="{{_options.selectedKey}}">
   </ws:Controls.List.ListControl.ListView>

   <div class="ws-ListControl__bottomPlaceholder">
      <div class="ws-ListControl__bottomLoadTrigger"></div>
   </div>
   <div class="ws-ListControl__bottomListTrigger"></div>
</div>