<div class="controls-FilterPanelSelect">
   <ws:for data="index, item in _options.items">
            <span if="{{index}}" class="controls-FilterPanelSelect__separator"> / </span>
      <Controls.Button style="linkMain" caption="{{item.value}}"
                       on:click="_clickHandler(item)"/>
   </ws:for>
</div>