<div>
    <ws:partial template="{{_options.content}}" scope="{{_options}}" selectedKeys="{{_selectedKeys}}" on:selectionChange="_selectionChange()"/>
    <Controls.Event.Emitter event="selectComplete" on:selectComplete="_selectComplete()" />
</div>