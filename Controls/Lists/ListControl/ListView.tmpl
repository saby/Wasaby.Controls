<ws:Controls.Lists.ListControl.ListRender event:onItemClick="_onItemClick()"
      display="{{_display}}"
      itemTemplate="{{_itemTemplate}}"
      displayProperty="{{_options.displayProperty}}"
      selectedItem="{{_selectedItem}}">
</ws:Controls.Lists.ListControl.ListRender>