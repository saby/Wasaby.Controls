<div class="controls-Toolbar__item
            controls-Toolbar__item{{buttonTemplateOptions.readOnly ? '_readOnly'}}
            {{!isFirstItem ? 'controls-Toolbar__item_spacing_' + itemsSpacing}}"
     title="{{item.tooltip}}">
   <ws:partial template="{{buttonTemplate}}"
               scope="{{buttonTemplateOptions}}"
               theme="{{theme}}"
               attr:class="controls-Toolbar__button"/>
</div>
