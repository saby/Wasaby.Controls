<div>
    <Controls.BreadCrumbs items="{{_options.items}}"
                          visibleItems="{{_visibleItems}}"
                          idProperty="{{_options.idProperty}}"
                          displayProperty="{{_options.displayProperty}}"
                          on:itemClick="_onItemClick()"/>

    <Controls.Event.Emitter event="resize"
                            on:resize="_onResize()"/>
</div>