<!doctype html>
<Controls.Application.Core
   application="{{application}}"
   scope="{{_options}}"
   attr:component="{{application}}"
   attr:data-component="Controls/Application/Core"
   attr:key="_"
   cssLinks="{{ cssLinks }}"
   wsRoot="{{wsRoot}}"
   appRoot="{{appRoot}}"
   resourceRoot="{{resourceRoot}}"
   jsLinks="{{ jsLinks }}"
   cssBundles="{{ cssBundles }}"
   servicesPath="{{ servicesPath }}"
   product="{{ product }}"
   buildnumber="{{ buildnumber }}"/>
