<!doctype html>
<ws:partial
   attr:component="{{template}}"
   template="{{template}}"
   scope="{{_options}}"
   cssLinks="{{ cssLinks }}"
   wsRoot="{{wsRoot}}"
   resourceRoot="{{resourceRoot}}"
   jsLinks="{{ jsLinks }}"  />