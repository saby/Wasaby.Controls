<body>
    <Controls.popup:Dialog
        name="dialogOpener"
        on:close="_onClose()"
    />
    <ws:partial
        template="{{ _options.content }}"
        scope="{{ _options }}"
        on:serviceError="_onServiceError()"
    />
</body>
