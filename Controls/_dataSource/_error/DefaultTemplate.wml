<Controls.Popup.Opener.Confirmation.Dialog
    type="ok"
    style="danger"
    message="{{ _options.message || message }}"
    details="{{ _options.details || details }}"
    closeHandler="{{ _closeHandler }}"
/>
