<div class="controls-Path">
    <div class="controls-Path__wrapper">
        <ws:if data="{{_options.items}}">
            <div class="controls-Path__backButton__wrapper {{_backButtonClass}}">
                <Controls.Button.Back attr:class="controls-Path__backButton"
                                      caption="{{_backButtonCaption}}"
                                      style="primary"
                                      on:click="_onBackButtonClick()"/>
                <div class="controls-Path__backButtonArrow icon-View icon-primary{{_breadCrumbsItems.length > 0 ? '' : ' controls-Path__backButtonArrow_outsideBackButton'}}"
                     on:click="_onArrowClick()"></div>
            </div>
            <Controls._BreadCrumbsView attr:class="controls-Path__breadCrumbs {{_breadCrumbsClass}}"
                                  items="{{_breadCrumbsItems}}"
                                  visibleItems="{{_visibleItems}}"
                                  keyProperty="{{_options.keyProperty}}"
                                  displayProperty="{{_options.displayProperty}}"
                                  on:itemClick="_onItemClick()"/>
        </ws:if>

        <Controls.Event.Emitter event="resize"
                                on:resize="_onResize()"/>
    </div>
</div>