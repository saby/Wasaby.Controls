<ws:if data="{{_items}}">
   <ws:partial template="{{_options.content}}"
               items="{{_items}}"
               on:itemClick="_itemClickHandler()"
   />
</ws:if>
<ws:else>
   <invisible-node/>
</ws:else>
