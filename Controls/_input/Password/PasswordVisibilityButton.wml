<ws:if data="{{isVisibleButton()}}">
   <div title="{{isVisiblePassword() ? rk('Скрыть') : rk('Показать')}}"
        class="controls-PasswordInput__showPassword controls-PasswordInput__showPassword_theme_{{getTheme()}} {{isVisiblePassword() ? 'icon-Hide' : 'icon-ShowBig'}}"
        on:click="_toggleVisibilityHandler()">
   </div>
</ws:if>
