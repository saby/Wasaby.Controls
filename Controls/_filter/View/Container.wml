<!-- TODO: Удалить опцию items и подписку на itemsChanged после перехода всех на filter:View-->
<ws:partial template="{{_options.content}}"
            items="{{_options.filterButtonItems}}"
            source="{{_options.filterButtonItems}}"
            historyId="{{_options.historyId}}"
            on:sourceChanged="_sourceChanged()"
            on:itemsChanged="_sourceChanged()"
            on:filterChanged="_filterChanged()"
            on:historyApply="_historyApply()"/>
