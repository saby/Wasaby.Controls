<ws:partial template="{{ itemTemplate }}"
            type="{{ type }}"
            active="{{selectedKey === currentItemId}}"
            attr:class="{{ selectedKey !== currentItemId ? 'ws-hidden' }}">
   <ws:templateOptions type="{{ type }}" active="{{selectedKey === currentItemId}}"/>
</ws:partial>