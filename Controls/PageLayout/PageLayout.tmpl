<div class="controls-PageLayout">
    <div attr:class="controls-PageLayout_wrapper">
        <ws:if data="{{_controlsAreaTemplate}}">
            <ws:partial attr:class="controls-PageLayout__ControlsArea" template="{{_controlsAreaTemplate}}"/>
        </ws:if>
        <ws:if data="{{_options.tabsSource}}">
            <div attr:class="controls-PageLayout__ButtonTabs_wrapper">
                <Controls.Tabs.Buttons
                        bind:selectedKey="_options.tabsSelectedKey"
                        keyProperty="id"
                        source="{{_options.tabsSource}}"
                />
            </div>
        </ws:if>
    </div>
    <Controls.Container.Async templateName="{{_tabContent || _options.content}}" templateOptions="{{_options}}"/>
</div>