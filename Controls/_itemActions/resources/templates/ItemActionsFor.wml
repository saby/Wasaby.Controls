<ws:for data="action in showedActions">
    <ws:if data="{{action.isMenu}}">
        <Controls.dropdown:Button scope="{{itemData.getMenuButtonConfig()}}"/>
    </ws:if>
    <ws:else>
        <ws:partial
                template="wml!Controls/_itemActions/resources/templates/ItemAction"
                itemData="{{itemData}}"
                size="{{size}}"
                action="{{action}}"/>
    </ws:else>
</ws:for>
