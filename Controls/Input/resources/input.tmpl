<input
   name="input"
   type="{{ type }}"
   value="{{value}}"
   class="controls-InputRender__field"
   placeholder="{{enabled ? placeholder}}"
   readonly="{{enabled ? '' : 'readonly'}}"
   autocomplete="off"
/>