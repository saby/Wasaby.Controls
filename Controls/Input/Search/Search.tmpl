<Controls.Input.resources.InputRender.InputRender
                value="{{_options.value}}"
                tagStyle="{{_options.tagStyle}}"
                viewModel="{{_baseViewModel}}"
                placeholder="{{_options.placeholder}}"
                inputType="Search"
                style="{{_options.style}}"
                on:valueChanged="_valueChangedHandler()">

   <ws:content>
      <ws:partial
              template="tmpl!Controls/Input/resources/input"
              on:keydown="_keyDownHandler()"
      />
   </ws:content>

   <ws:afterFieldWrapper>

      <div class="controls-Search__afterFieldWrapper">
         <div if="{{_options.value}}"
              class="controls-Search__reset icon-Close"
              title="{{rk('Очистить результаты поиска')}}"
              on:click="_onResetClick()"></div>
         <div class="controls-Search__search controls-Search__search_state{{!isEnabled() ? '_disabled' : '_default'}} icon-Search2"
              on:click="_onSearchClick()"></div>
      </div>
   </ws:afterFieldWrapper>

</Controls.Input.resources.InputRender.InputRender>