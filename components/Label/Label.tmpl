<div
   class="controls-Label controls-Label__text controls-Label__text_color controls-Label__text_size{{_options.visible ? '' : ' ws-hidden'}}"
   title="{{_options.tooltip}}"
   on:click="_clickCaptionHandler()"
>{{_options.caption|sanitize}}</div>