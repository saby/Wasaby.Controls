<span class="controls-ButtonBase controls-SelectorButton {{!visible ? ' ws-hidden'}}" title="{{tooltip}}" disabled="{{!enabled ? 'disabled'}}" type="button">
    <span class="controls-ButtonBase__content">
        <ws:partial template="{{contentTemplate}}" scope="{{...}}"></ws:partial>
    </span>
</span>