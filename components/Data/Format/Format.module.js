/* global define, $ws */
define('js!SBIS3.CONTROLS.Data.Format.Format', ['js!WS.Data.Format.Format'], function (Format) {
   'use strict';
   $ws.single.ioc.resolve('ILogger').error('SBIS3.CONTROLS.Data.Format.Format', 'Module is no longer available since version 3.7.4.100. Use WS.Data.Format.Format instead.');
   return Format;
});
