<td class="controls-DataGridView__td controls-DataGridView__firstContentCell" colspan="{{viewCfg.colspan}}">
   <SBIS3.CONTROLS.BreadCrumbs class="controls-BreadCrumbs__smallItems controls-BreadCrumbs__home-hide controls-TreeView__searchBreadCrumbs"
                                  items="{{path}}"
                                  itemTpl="{{bcTpls.itemTpl}}"
                                  itemContentTpl="{{bcTpls.itemContentTpl}}"
                                  highlightEnabled="{{viewCfg.highlightEnabled}}"
                                  highlightText="{{viewCfg.highlightText}}"
                                  colorMarkEnabled="{{viewCfg.colorMarkEnabled}}"
                                  colorField="{{viewCfg.colorField}}"
                                  enabled="{{viewCfg.allowEnterToFolder || !!viewCfg.task1173671799}}"
                                  idProperty="{{viewCfg.idProperty}}"
                                  displayProperty="{{viewCfg.displayProperty}}"
                                  activableByClick="{{false}}"
                                  allowChangeEnable="{{false}}"
                                  command="BreadCrumbsItemClick"
         />
   </td>
