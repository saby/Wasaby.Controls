<tr class="controls-GroupBy{{(stickyHeader) ? ' ws-sticky-header__table-sticky-row' : ''}}" data-group="{{groupId}}">
    <td colspan="{{colspan}}">
        <ws:partial template="{{groupContentTemplate}}" scope="{{...}}" />
    </td>
</tr>