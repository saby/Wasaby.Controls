<div class="controls-Columns-PresetsList">
   <ws:SBIS3.CONTROLS.DropdownList
      name="controls-Columns-PresetsList__dropdown"
      className="controls-Columns-PresetsList__dropdown"
      multiselect="{{false}}"
      mode="click"
      type="titleHeader"
      title="{[Выберите шаблон]}"
      idProperty="title"
      displayProperty="title"
   />
</div>