<div class="controls-Menu controls-Menu__Popup">
   <div class="controls-Menu__itemsContainer"></div>
   <ws:SBIS3.CONTROLS.CommandsSeparator
      name="toggleAdditionalItems"
      if="{{additionalProperty}}"
      command="toggleAdditionalItems"
   />
   <div if="{{footerTpl}}" class="controls-Menu__footer">{{footerTpl()}}</div>
</div>