<div class="controls-ListView__item js-controls-ListView__item {{_getItemData(projItem).selected ? 'controls-ListView__item__selected'}} {{ hovered ? 'controls-ListView__item__hovered' }} {{ multiSelected ? 'controls-ListView__item__multiSelected' }} {{ className }} "
     data-hash="{{ projItem.getHash() }}" event:onclick="_onItemClick(projItem)">
    <ws:partial template="{{contentTemplate}}" scope="{{...}}" attributes="{{ attributes }}"></ws:partial>
</div>
