<ws:for data="index,item in records">
    <ws:if data="{{ item.getContents }}">
        <ws:partial
                template="{{_tplData.itemTpl}}"
                scope="{{_tplData}}"
                item="{{item.getContents()}}"
                projItem="{{item}}"
                selected="{{_tplData.selectedKey == _tplData.getPropertyValue(item.getContents(), _tplData.idProperty)}}"
                multiSelected="{{ item.isSelected() }}"
                hovered="{{index === hoveredIndex}}"/>
    </ws:if>
    <ws:else>
        <ws:partial template="{{ item.tpl }}" scope="{{ item.data }}" />
    </ws:else>
</ws:for>
