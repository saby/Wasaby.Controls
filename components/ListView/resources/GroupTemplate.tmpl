<div class="controls-GroupBy{{groupCollapsing[groupId] ? ' controls-GroupBy__collapsed' : ''}}" data-group="{{groupId}}" data-group-hash="{{groupHash}}">
    <ws:partial template="{{ groupContentTemplate }}" scope="{{...}}"></ws:partial>
</div>
