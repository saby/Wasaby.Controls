<button class="controls-ButtonBase{{!visible ? ' ws-hidden'}}" title="{{tooltip}}" disabled="{{!enabled ? 'disabled'}}" type="button">
    <ws:partial template="{{contentTemplate}}" scope="{{...}}"></ws:partial>
</button>