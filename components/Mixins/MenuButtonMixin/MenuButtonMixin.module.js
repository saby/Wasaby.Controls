define('js!SBIS3.CONTROLS.MenuButtonMixin', ['Core/IoC'], function(IoC) {

    IoC.resolve('ILogger').info('Deprecated', 'модуль MenuButtonMixin объявлен deprecated и будет удален в версии 3.7.5.200. Вся логика перенесена в WSControls/Buttons/MenuButton');

    var MenuButtonMixin = {
    }
    return MenuButtonMixin;
});