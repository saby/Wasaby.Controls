<ws:if data="{{tplData.viewMode === 'list' || tplData.viewMode === 'tile'}}">
   <ws:partial
      template="{{tplData._itemsTemplate}}"
      records="{{records}}"
      tplData="{{tplData}}"
   />
</ws:if>