<div class="controlsDemo__wrapper">
    <Controls.list:View
            name="list"
            keyProperty="id"
            source="{{_viewSource}}"
            markerVisibility="onactivated"/>

    <Controls.buttons:Button
            readOnly="{{false}}"
            icon="icon-small icon-TurnL"
            caption="Reload list"
            on:click="_reloadList()"/>
</div>