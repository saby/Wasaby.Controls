<div>
   <h1>WrapURLs</h1>
   <Controls.input:Area minLines="{{5}}" bind:value="_value" on:inputCompleted="_inputCompletedHandler()"/>

   <ws:if data="{{_wrapText}}">
      <Controls.decorator:WrapURLs text="{{_wrapText}}"/>
   </ws:if>
</div>