<div class="controlsDemo__wrapper">
    <Controls.treeGrid:View
            keyProperty="id"
            source="{{_viewSource}}"
            columns="{{_columns}}"
            parentProperty="parent"
            colspan="{{true}}"
            nodeProperty="type">
        <ws:colspanTemplate colspan="{{true}}"/>
        <ws:editingConfig editOnClick="{{true}}" enabled="{{true}}" colspan="{{true}}"/>
    </Controls.treeGrid:View>
</div>