<div class="root controlsDemo_fixedWidth500 {{_options.theme !== 'default' ? ' root__dark_background'}}">
    <div class="separator">inlineHeight = 's', borderThickness = 's'</div>
    <div class="demo-TabButtons ControlsDemo-Tabs__rightAlign">
        <Controls.tabs:Buttons
                bind:selectedKey='SelectedKey1'
                keyProperty="id"
                items="{{_items1}}"
        />
    </div>
    <div class="separator">inlineHeight = 'l', borderThickness = 'l'</div>
    <div class="demo-TabButtons ControlsDemo-Tabs__mainSeparate">
        <Controls.tabs:Buttons
                bind:selectedKey='SelectedKey2'
                keyProperty="id"
                inlineHeight="l"
                borderThickness="l"
                items="{{_items1}}"
        />
    </div>
    <div class="separator">inlineHeight = 's', borderThickness = 'l'</div>
    <div class="demo-TabButtons ControlsDemo-Tabs__mainSeparate">
        <Controls.tabs:Buttons
                bind:selectedKey='SelectedKey3'
                keyProperty="id"
                borderThickness="l"
                items="{{_items1}}"
        />
    </div>
</div>
