<ws:partial template="Controls/grid:ColumnTemplate" scope="{{_options}}">
   <ws:contentTemplate>
      <ws:if data="{{itemData.item.sum}}">
         <Controls.decorator:Money value="{{itemData.item.sum}}"
                                   fontColorStyle="unaccented"
                                   fontSize="l"
                                   fontWeight="bold"
         />
      </ws:if>
   </ws:contentTemplate>
</ws:partial>
