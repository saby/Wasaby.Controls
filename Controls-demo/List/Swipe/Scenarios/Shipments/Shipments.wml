<Controls.grid:View
   displayProperty="title"
   keyProperty="id"
   source="{{_source}}"
   columns="{{_columns}}"
   swipeDirection="column"
   titlePosition="right"
   itemActions="{{_itemActions}}" />