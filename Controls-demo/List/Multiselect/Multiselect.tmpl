<div class="123">
    <h2>Default</h2>
    <Controls:List
            source = "{{_viewSource}}"
            markedKey="{{2}}"
            selectedKeys="{{[1, 3]}}"
            allowEmptySelection="{{false}}"
            idProperty="id">
    </Controls:List>
</div>