<div on:click="clickHandler()">
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     caption="test header h2"
                     style="default_big"
                     clickable="{{false}}"
                     counterValue="{{12}}"
                     counterLocation="after"
                     counterStyle="primary"
                     counterSize="h6"
                     countClickable="{{true}}"/>
    <span>base line</span>
    <br/>
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     caption="test header h2"
                     style="primary_big"
                     clickable="{{false}}"
                     counterValue="{{12}}"
                     counterLocation="before"
                     counterStyle="default"
                     counterSize="h6"
                     countClickable="{{true}}"/>
    <span>base line</span>
    <br/>
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     size="default"
                     caption="test header h3"
                     style="default"
                     clickable="{{false}}"
                     counterValue="{{12}}"
                     counterLocation="before"
                     counterStyle="disabled"
                     counterSize="h7"
                     countClickable="{{false}}"/>
    <span>base line</span>
    <br/>
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     size="l"
                     caption="test header h3"
                     style="primary"
                     clickable="{{true}}"
                     counterValue="{{12}}"
                     counterLocation="before"
                     counterStyle="default"
                     counterSize="h6"
                     countClickable="{{false}}"
                     iconClickable="{{false}}"
                     iconStyle="Main"
                     icon="icon-16 icon-MarkExpandBold "/>
    <span>base line</span>
    <br/>
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     size="s"
                     caption="test header h3"
                     style="default"
                     clickable="{{true}}"
                     counterValue="{{12}}"
                     counterLocation="after"
                     counterStyle="primary"
                     counterSize="h7"
                     countClickable="{{true}}"
                     iconClickable="{{true}}"
                     iconStyle="Accent"
                     icon="icon-16 icon-ExpandLight "/>
    <span>base line</span>
</div>