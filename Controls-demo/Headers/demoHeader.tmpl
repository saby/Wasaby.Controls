<div on:click="clickHandler()">
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     caption="test header h2"
                     style="default_big"
                     clickable="{{false}}"
                     counterValue="{{12}}"
                     counterLocation="after"
                     counterStyle="primary"
                     counterSize="default"
                     countClickable="{{true}}"/>
    <span>base line</span>
    <br/>
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     caption="test header h2"
                     style="primary_big"
                     clickable="{{false}}"
                     counterValue="{{12}}"
                     counterLocation="before"
                     counterStyle="default"
                     counterSize="default"
                     countClickable="{{true}}"/>
    <span>base line</span>
    <br/>
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     size="default"
                     caption="test header h3"
                     style="default"
                     clickable="{{false}}"
                     counterValue="{{12}}"
                     counterLocation="before"
                     counterStyle="disabled"
                     counterSize="s"
                     countClickable="{{false}}"
                     iconLocation="before"
                     iconClickable="{{false}}"
                     iconStyle="Main"
                     iconType="MarkExpandBold"
                     iconValue="{{_iconValue}}"/>
    <span>base line</span>
    <br/>
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     size="l"
                     caption="test header h3"
                     style="primary"
                     clickable="{{true}}"
                     counterValue="{{12}}"
                     counterLocation="before"
                     counterStyle="default"
                     counterSize="default"
                     countClickable="{{true}}"
                     iconLocation="after"
                     iconClickable="{{false}}"
                     iconStyle="Main"
                     iconType="MarkExpandBold"
                     iconValue="{{_iconValue}}"
                     commonClick="{{true}}"/>
    <span>base line</span>
    <br/>
    <span>base line</span>
    <Controls:Header on:countClick="clickCount()"
                     on:iconClick="iconCount()"
                     size="s"
                     caption="test header h3"
                     style="default"
                     clickable="{{true}}"
                     counterValue="{{12}}"
                     counterLocation="after"
                     counterStyle="primary"
                     counterSize="s"
                     countClickable="{{true}}"
                     iconClickable="{{true}}"
                     iconLocation="before"
                     iconStyle="Accent"
                     iconType="ExpandLight"
                     iconValue="{{_iconValue}}"
                     separatorIcon="{{true}}"
                     separatorIconStyle="primary"/>
    <span>base line</span>
    <br/>
    <Controls:Button:BackButton:Back style="primary" size="l" caption="backButton l primary"/>
    <br/>
    <Controls:Button:BackButton:Back style="default" size="s" caption="backButton s default"/>
    <br/>
    <Controls:Button:BackButton:Back style="primary" size="default" caption="backButton default primary"/>
</div>