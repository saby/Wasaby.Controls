<ws:if data="{{ column.result }}">
   <Controls.decorator:Money value="{{column.result}}"
                             style="{{column.displayProperty === 'sum' ? 'accentResults' : 'noAccentResults'}}"
                             delimiters="{{true}}"/>
</ws:if>
<ws:else>
   <invisible-node />
</ws:else>
