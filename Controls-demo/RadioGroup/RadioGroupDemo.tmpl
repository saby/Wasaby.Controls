<div class="wrapper">
    <Controls.Toggle.Radio
            on:selectKeyChange="changeKey()"
            keyProperty="id"
            selectedKey="{{_selectKey}}"
            source="{{_source}}"
            direction="vertical"
            readOnly="{{false}}"
            style="default"/>
</div>