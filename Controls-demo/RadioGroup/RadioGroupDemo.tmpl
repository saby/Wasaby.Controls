<div class="demo-RadioGroup__Wrapper">
    <div class="demo-RadioGroup__TestBlock">
        <h2>Верстка по умолчанию. Задан displayProperty.</h2>
        <Controls.Toggle.Radio
                attr:id="radioGroup3"
                attr:class="demo-RadioGroup"
                on:selectedKeyChanged="changeKey()"
                keyProperty="id"
                selectedKey="{{_selectKey}}"
                source="{{_source}}"
                direction="horizontal"
                readOnly="{{false}}">
            <ws:itemTemplate>
                <ws:partial template='tmpl!Controls/Toggle/Radio/resources/ItemTemplate'
                            item="{{item}}"
                            readOnly="{{readOnly}}"
                            selected="{{selected}}"
                            displayProperty="caption"
                />
            </ws:itemTemplate>
        </Controls.Toggle.Radio>
    </div>
    <div class="demo-RadioGroup__TestBlock">
        <h2>Для каждого элемента используется верстка по умолчанию. В режиме чтения.</h2>
        <Controls.Toggle.Radio
                attr:id="radioGroup1"
                on:selectedKeyChanged="changeKey()"
                attr:class="demo-RadioGroup"
                keyProperty="id"
                selectedKey="{{_selectKey}}"
                source="{{_source}}"
                direction="vertical"
                readOnly="{{true}}"/>
    </div>
    <div class="demo-RadioGroup__TestBlock">
        <h2>Задан ws:contentTemplate. Для каждого заголовка используется верстка заданная в опциях.</h2>
        <Controls.Toggle.Radio
                attr:id="radioGroup2"
                attr:class="demo-RadioGroup"
                on:selectedKeyChanged="changeKey()"
                keyProperty="id"
                selectedKey="{{_selectKey}}"
                source="{{_source}}"
                direction="horizontal"
                readOnly="{{false}}">
            <ws:itemTemplate>
                <ws:partial template='tmpl!Controls/Toggle/Radio/resources/ItemTemplate'
                            item="{{item}}"
                >
                    <ws:contentTemplate>
                        <div style="padding: 4px;">
                            <div style="font-size: 11px; color: grey">{{itemTemplate.item.get('title')}}</div>
                        </div>
                    </ws:contentTemplate>
                </ws:partial>
            </ws:itemTemplate>
        </Controls.Toggle.Radio>
    </div>
    <div class="demo-RadioGroup__TestBlock">
        <h2>Для каждого элемента используется верстка по умолчанию. Для 3 элемента (itemTemplateProperty === 'templateTwo') задана персональная верстка. В режиме чтения.</h2>
        <Controls.Toggle.Radio
                attr:id="radioGroup1"
                on:selectedKeyChanged="changeKey()"
                attr:class="demo-RadioGroup"
                keyProperty="id"
                selectedKey="{{_selectKey}}"
                source="{{_source}}"
                direction="vertical"
                readOnly="{{true}}"
                itemTemplateProperty="templateTwo"/>
    </div>
</div>