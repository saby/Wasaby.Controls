<ws:template name="kind">
   <Controls.filterPopup:Dropdown
           keyProperty="key"
           displayProperty="title"
           bind:textValue="item.textValue"
           bind:selectedKeys="item.value"
           source="{{item.source}}"/>
</ws:template>

<ws:template name="type">
<Controls.filterPopup:Dropdown
        keyProperty="key"
        displayProperty="title"
        bind:textValue="item.textValue"
        bind:selectedKeys="item.value"
        source="{{item.source}}"/>
</ws:template>

<ws:template name="own">
<Controls.filterPopup:Dropdown
        keyProperty="key"
        displayProperty="title"
        bind:textValue="item.textValue"
        bind:selectedKeys="item.value"
        source="{{item.source}}"/>
</ws:template>

<ws:template name="used">
<Controls.filterPopup:Dropdown
        keyProperty="key"
        displayProperty="title"
        bind:textValue="item.textValue"
        bind:selectedKeys="item.value"
        source="{{item.source}}"/>
</ws:template>

<ws:template name="deleted">
<Controls.toggle:Checkbox bind:value="item.value" caption="Show deleted" attr:style="margin-left: -5px;"/>
</ws:template>

<ws:partial template="{{item.id}}" item="{{item}}"/>