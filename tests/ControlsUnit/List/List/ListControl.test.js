define([
   'Controls/list',
   'Core/Deferred',
   'Core/core-instance',
   'Controls/Application/SettingsController'
], function(
   lists,
   Deferred,
   cInstance,
   SettingsController
) {
   describe('Controls.List.ListControl', function() {

   });
});
